import React from "react";

const Button= () => (

);

function PropTypes(){
    return (
        <>
            <Button text={} fontSize={} />
            <Button text={} fontSize={} />
        </>
    );
};